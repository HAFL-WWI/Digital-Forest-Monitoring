import {removeContent} from "./main_util"
const description_util = {
  model: {
    
  },
  controller: {
    /*
     * calls the necessary functions to display the projektbescchrieb.
     */
    init: () => {
      removeContent();
      
    },
    
  },
  view: {
    
  }
};

export default description_util;
